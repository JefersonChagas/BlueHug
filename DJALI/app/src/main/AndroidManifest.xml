<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.djali">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" /> <!-- Permissão de acesso a internet -->

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:icon="@mipmap/icon_app_foreground"
        android:theme="@style/Theme.AppCompat.Translucent">
        <activity android:name=".Medicos.TelaFeedback"></activity>
        <activity android:name=".Medicos.Tela_Coment" />
        <activity android:name=".Mapa_A" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".Medicos.Espec_A" />
        <activity
            android:name=".TelaDados"
            android:label="@string/title_activity_tela_dados"
            android:theme="@style/Theme.AppCompat.Translucent.NoActionBar" />
        <activity
            android:name=".TelaPrincipal"
            android:label="@string/app_name"
            android:theme="@style/Theme.AppCompat.Translucent.NoActionBar" />
        <activity
            android:name=".ui.login.LoginActivity"
            android:label="DJALI" />
        <activity android:name=".tela_cadastro" />
        <activity
            android:name=".SplashActivity"
            android:theme="@style/Theme.AppCompat.Translucent.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>